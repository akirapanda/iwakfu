<%= simple_form_for(@mob_family,:html=>{:class=>"form-horizontal"}) do |f| %>
	<fieldset>
		<legend>新建新的怪物家族记录</legend>
		<%= f.file_field :icon %>
		<%= f.input :name%>
		<%= f.submit %>
	</fieldset>
<%end%>